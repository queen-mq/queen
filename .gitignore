node_modules
.env
*.env
.env*

beanchmark/bin

# Built webapp for npm package (copied during prepublishOnly)
src/webapp-dist
webapp/node_modules
webapp/dist
app/dist
app/node_modules

server/vendor
server/build
server/bin

clients/client-js/node_modules

clients/client-cpp/bin

lib/build


helm
helm_queen
helm_v1
upgrade.sh
upgrade_queen.sh
start.sh
.DS_Store

# Rust build artifacts (release/debug)
server-rust/target
server/target
**/target/

server/webapp/dist

# dev-cell runtime artefacts (scripts/dev-cell.sh): logs and pid files of a
# throwaway local stack, rewritten on every run.
proxy/.devcell/
