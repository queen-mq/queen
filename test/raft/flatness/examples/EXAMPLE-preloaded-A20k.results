# EXAMPLE FILE — INVENTED numbers, see EXAMPLE-empty-A20k.results. This one
# shows what a FAILING flatness run looks like: latency and CPU follow the
# stored volume instead of the write rate (the G-3 regression), RSS drifts
# inside the run (the I8 half), and the snapshot build grew — which is reported
# and NOT gated.
# regime=A20k state=preloaded host=EXAMPLE-not-a-measurement commit=EXAMPLE topology=raft3 duration_s=3600 command="goload -mode openloop -rate 20000 -partitions 64"
p50_ms               26.4
p99_ms               44.0
p999_ms              131
ack_rtt_ms           23.9
cpu_cores            4.9
disk_mbps            121
durable_point_ms     5.1
snapshot_build_s     390
rss_start_mb         1180
rss_end_mb           1490
