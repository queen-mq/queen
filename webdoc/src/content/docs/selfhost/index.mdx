---
title: Self-hosting Queen
description: "Deploy and operate Queen: one binary, one PostgreSQL, and the operational detail that matters."
type: overview
tier: operate
sidebar:
  order: 1
sourceOfTruth:
  - server/src/main.rs
  - server/src/config.rs
  - server/src/schema.rs
  - server/src/mesh.rs
  - server/Cargo.toml
  - Dockerfile
---

A Queen deployment is one process and one database. The broker is a single
stateless Rust binary (crate `queen`, binary `queen`) that serves HTTP on port
6632 and keeps every byte of state in PostgreSQL. No broker-local data directory
to back up, no coordination service, no sidecar, no message file format on disk.

A complete deployment, ready to verify:

```bash
docker network create queen
docker run -d --name queen-pg --network queen -e POSTGRES_PASSWORD=postgres postgres:16
docker run -d --name queen --network queen -p 6632:6632 -e PG_HOST=queen-pg -e PG_PASSWORD=postgres -v queen-spool:/var/lib/queen/buffers ghcr.io/queen-mq/queen:latest
curl -s http://localhost:6632/health
```

[Deploy a broker](/selfhost/deploy) walks through each line and the four-step
verification that proves the whole path. Everything else is optional, and each
optional piece is off or inert until you configure it.

## What a deployment is made of

| Piece | Required? | What it is |
| --- | --- | --- |
| `queen` broker | Yes | One process. Stateless. Scales to 1–3 identical replicas. |
| PostgreSQL | Yes | The only source of truth: messages, offsets, dedup index, queue config, DLQ. |
| Schema | Applied for you | The DDL and 23 stored-procedure files are compiled into the binary and re-applied idempotently at every boot, under an advisory lock. No migration step, no extensions. |
| Dashboard | Bundled | The SPA is compiled into the binary with `rust_embed` and served from the router's fallback. There is no static-directory override. |
| `queenctl` | Bundled in the image | The operator CLI, on `PATH` inside the published container with `QUEEN_SERVER` pre-set. |
| Disk spool | On by default | Pushes are appended to `FILE_BUFFER_DIR` when PostgreSQL is unreachable, and replayed when it comes back. |
| Mesh | Optional, inert alone | Framed TCP between replicas, carrying best-effort hints only. A single broker with no peers binds nothing. |
| JWT authentication | Optional, **off** | `JWT_ENABLED=false` by default: the middleware is a transparent pass-through. |
| TLS | Not provided | The broker has no HTTPS listener. Terminate TLS in front of it. |
| `queen_proxy` | Optional | A separate binary: multi-tenant gateway with its own PostgreSQL, API keys, quotas, rate limits and metering. |

## The broker's boundaries

Read this list before you put a broker anywhere a client can reach it.

- **TLS is terminated in front.** `rustls` is linked in only as a *client* (for
  PostgreSQL and JWKS fetches); nothing in the broker accepts an HTTPS
  connection. Termination is a reverse proxy's job, or `queen_proxy`'s.
- **Cross-origin browser calls are not served.** There is no `Access-Control-*`
  handling in the request path.
- **Authentication is opt-in.** With `JWT_ENABLED` unset, every route is served
  to every caller, including `/api/v1/system/*` and `/internal/*`: a broker on a
  routable address with default configuration is fully administrable by anyone
  who can reach it.
- **Admission control is `queen_proxy`'s job.** The broker applies no rate
  limiting and no quotas, and has no 429 path at all.
- **Every replica serves every route.** There is no leader; only background
  maintenance is leader-gated, by PostgreSQL advisory locks, per cycle.

## Four behaviours to know before you deploy

Each is explained in full on the page that owns it.

- `/health` answers **503** during a database outage by design: readiness, never
  liveness. See
  [the liveness-probe trap](/selfhost/probes-restarts#the-liveness-probe-trap).
- The mesh port must be firewalled to the broker peers; a requirement, not a
  tip. See [why](/selfhost/ha#the-mesh-port-must-be-firewalled).
- A malformed `QUEEN_ENCRYPTION_KEY` silently **disables encryption**: flagged
  queues store plaintext. See
  [the silent failure](/selfhost/security/encryption#a-bad-key-silently-stores-plaintext).
- `POST /api/v1/configure` is a **full replace**: omitted keys reset to their
  defaults. See [queue options](/reference/queue-options).

## Start here

<CardGrid>
  <LinkCard
    href="/selfhost/deploy"
    title="Deploy a broker"
    description="Docker and from source: the image contents, the environment variables that matter, and verification steps that actually prove it works."
  />
  <LinkCard
    href="/selfhost/postgres"
    title="PostgreSQL"
    description="Version, privileges, why a direct connection is required, pool sizing, and the server settings that matter for this workload."
  />
  <LinkCard
    href="/selfhost/configuration"
    title="Configuration"
    description="How the broker reads its environment, the strict boolean parser, the boot-time effective-config block, and the handful of knobs worth touching."
  />
  <LinkCard
    href="/selfhost/ha"
    title="High availability"
    description="1–3 replicas against one PostgreSQL, what the mesh carries and what it does not, and why the mesh port must be firewalled."
  />
  <LinkCard
    href="/selfhost/upgrade"
    title="Upgrades"
    description="Idempotent schema re-apply, rolling replicas, what happens to in-flight leases, and why 0.16 is not a data migration."
  />
  <LinkCard
    href="/selfhost/kubernetes"
    title="Kubernetes"
    description="A complete reference manifest written for this broker, with probes that are not coupled to the database."
  />
</CardGrid>

## The rest of this section

The pages above get a broker running and keep it running. The rest go deeper on
single subjects, each worth reading before you rely on the behaviour it
describes:

- [Security](/selfhost/security/): trust boundaries, JWT algorithms and JWKS key
  discovery, broker-to-PostgreSQL TLS, at-rest payload encryption.
- [Observability](/selfhost/observability/): the log's periodic `rates` and
  `sizes` blocks, and the metrics endpoints.
- [Probes and restarts](/selfhost/probes-restarts/): health semantics, SIGTERM
  draining, rolling restarts.
- [Durability](/selfhost/durability/): surviving a PostgreSQL outage with the
  disk spool and maintenance mode.
- [Retention](/selfhost/retention/): what the maintenance cycle deletes, and how
  disk grows if you never turn retention on.
- [Sizing](/selfhost/sizing/): capacity planning in commits rather than
  messages.
- [Backup](/selfhost/backup/): `pg_dump`, because the database is the whole
  state.
- [Dashboard](/selfhost/dashboard/): what the bundled SPA shows.
- [Multi-tenant](/selfhost/multi-tenant/): tenants, API keys, quotas, metering
  and isolation on `queen_proxy`, plus the broker's `QUEEN_TENANCY_HEADER`.

The [route table](/reference/http/routes/) lists what the broker exposes on the
wire, the [configuration reference](/reference/config/) every environment
variable with the default the code applies, and [Benchmarks](/benchmarks) the
measured runs behind any performance claim.
