---
title: HTTP API conventions
description: "Base URL, which node answers in a cluster, authentication and access levels, error bodies and codes, the empty-204 rule and the body cap: the rules every Queen route follows."
type: reference
sidebar:
  order: 2
  label: Conventions
sourceOfTruth:
  - server/src/main.rs
  - server/src/auth.rs
  - server/src/config.rs
  - server/src/tenant.rs
  - server/src/handlers/mod.rs
  - server/src/handlers/data.rs
  - server/src/handlers/raft.rs
  - server/src/handlers/static_files.rs
  - server/src/handlers/standalone.rs
  - server/src/rsm/admit.rs
  - server/src/rsm/facade/mod.rs
  - server/src/rsm/facade/real.rs
  - server/src/rsm/facade/real/phase2.rs
digest: >-
  One HTTP API version, v1, the path itself being the version, with no negotiation. The broker
  binds QUEEN_BIND_ADDR (default 0.0.0.0) on PORT (default 6632), so a local base URL is
  http://localhost:6632. Its router has no TLS and no CORS layer. In a cluster any node serves
  clients: a follower forwards every /api/ and /streams/ request to the leader, except the
  /api/v1/raft/* views. Errors raised by the state machine are {"error","code"}: 503 with
  Retry-After: 1 for no_leader, retry and timeout; 429 overloaded with a Retry-After of 1 to 5 s
  once the push admission budget stays full for its hold; 507 storage_full above the disk
  high-water mark; 413 name_too_long; 400 with the planner's own code. A push answers 201 with a
  per-item status (queued, duplicate or error), so a 2xx does not mean every item was stored.
  Retry safety differs per route: a push is safe to retry blindly only when the caller sends its
  own transactionId (the retry answers duplicate and writes nothing); an ack and a lease extension
  are safe; a transaction is safe only with deterministic transactionIds; a pop never is, because
  a retry is a new claim; a kv incr never is, because it has no precondition and counts twice.
  Every 204 has no body at all, deliberately.
---

Queen's broker speaks plain HTTP/1.1 with JSON bodies. There is no binary protocol, no
persistent session, and no client-side coordination state. Anything that can issue an HTTP
request is a first-class client. The rules on this page hold for every route; the per-route
pages only document what is specific to that route.

## Base URL and versioning

The broker binds `PORT`, which defaults to **6632**, on `QUEEN_BIND_ADDR`, which defaults to
`0.0.0.0`. A local broker is therefore at `http://localhost:6632`. When the embedded proxy is
on (`QUEEN_PROXY_EMBEDDED=true`) it serves `PORT` itself unless `QUEEN_PROXY_PORT` names
another port; [Self-hosting](/deploy/) covers both layouts.

| Prefix | What lives there |
| --- | --- |
| `/api/v1/…` | the whole public API: message plane, queues, consumer groups, messages, DLQ, traces, status, analytics, system, the Raft views |
| `/streams/v1/…` | the stream-processing surface: three JSON POSTs, documented in [Streams routes](/reference/http/streams) |
| `/health`, `/metrics`, `/metrics/prometheus`, `/status` | operational endpoints |
| everything else | the embedded dashboard SPA, served from assets compiled into the binary |

There is exactly one API version, `v1`. Since the 1.0.0 line, broker and SDK minor versions are
released in alignment. A path is the API version: there is no `Accept` header negotiation and no
version query parameter.

<Aside type="caution">
The broker router has **no TLS listener and no CORS layer**. Put it behind something that
terminates TLS: the embedded proxy or your own reverse proxy. Do not expose the broker router's
port to a browser or to the internet directly. See [Self-hosting](/deploy/).
</Aside>

A path under `/api/` or `/streams/` that the broker does not serve answers a JSON **404**:

```json
{"code":"no_such_route","error":"not found","path":"/api/v1/does-not-exist"}
```

Any other path falls through to the dashboard, which answers a JSON 404
(`{"error":"Not Found","code":"no_such_route"}`) for any non-`GET`/`HEAD` request and for an
`/auth/` path it does not serve. A call to a route that does not exist therefore fails loudly
instead of returning the SPA's HTML with a 200.

## Which node answers

A single node answers every request itself. In a cluster, any node takes client requests: a
follower forwards every `/api/` and `/streams/` request to the leader and relays its answer, so a
client does not have to find the leader. The node you reached answers these itself:

- the `/api/v1/raft/*` views (`status`, `members`, `liveness`), because they describe that node;
- everything outside `/api/` and `/streams/`: `/health`, `/status`, `/metrics`,
  `/metrics/prometheus`, the dashboard and its `/auth/` routes.

While no leader is known (an election is running, or the cluster has no quorum) a request that
would be forwarded answers `503` with code `no_leader` and `Retry-After: 1`. A leader that cannot
be reached, or does not answer in time, gives `503` with code `retry`. Retry both.

## Authentication

JWT bearer authentication is **off by default** (`JWT_ENABLED=false`); with it off, every
route is reachable unauthenticated and the broker relies entirely on network isolation. With
it on, send the token in the standard header:

```text
Authorization: Bearer <jwt>
```

A bare token with no `Bearer ` prefix is also accepted. Supported algorithms are HS256/384/512,
RS256/384/512 and EdDSA, selected by `JWT_ALGORITHM` (or `auto`); the verification key comes
from a static PEM or from a JWKS endpoint cached per `kid`. Role claim names are remappable.
The [configuration page](/reference/config/) lists every `JWT_*` variable, and
[Self-hosting](/deploy/) covers key distribution.

When the embedded proxy serves `PORT` (`QUEEN_PROXY_EMBEDDED=true` without `QUEEN_PROXY_PORT`),
clients authenticate to the proxy, and the broker router behind it runs with its own JWT check
off and tenancy on. With `QUEEN_PROXY_PORT` set to another port, `PORT` serves the broker router
directly with the settings on this page.

Two failure codes, and they mean different things:

| Code | Meaning |
| --- | --- |
| `401` | no token, unparseable token, bad signature, expired, not yet valid, or a rejected issuer/audience. Body `{"error":"Authentication required"}` or a specific token message |
| `403` | the token is valid but its roles do not satisfy the route's access level. Body `{"error":"Insufficient permissions"}` |

### Access levels are a role set, not a ladder

Every route is mapped to one of five access levels. The mapping from a token's roles to the
levels it satisfies is **not** hierarchical: `write-only` is not a weaker `read-write`, it is a
different capability. This matters most for producers.

| Route's access level | Satisfied by role | Not satisfied by |
| --- | --- | --- |
| `public` | anyone, with or without a token | nothing |
| `read-only` | `read-only`, `read-write`, `admin` | `write-only` |
| `write-only` | `write-only`, `read-write`, `admin` | `read-only` |
| `read-write` | `read-write`, `admin` | `read-only`, `write-only` |
| `admin` | `admin` | everything else |

Three routes are `write-only`, and all three are pure produce operations: `POST /api/v1/push`,
`POST /api/v1/timers` (scheduling a timer produces the message it becomes) and
`POST /api/v1/ephemeral/push`. So a token whose single role is `write-only` can produce and can
do nothing else: every read route and every consume route (`/pop`, `/ack`, `/transaction`,
`/lease/:leaseId/extend`) is `read-write` and returns 403 for it. Conversely a `read-only` token
cannot pop, because popping takes a lease and mutates a cursor.

Role names come from the `role` claim and the `roles` array claim, and both the claim names and
the four role names are configurable. A path the access table does not recognise defaults to
`read-write`.

The public routes, reachable with no token even when authentication is on, are `/health`,
`/metrics`, `/metrics/prometheus`, `/`, `/assets/*`, `/favicon*`, and the three
dashboard-identity routes `/auth/me`, `/auth/login` and `/auth/logout`. The last three are
public so they can answer the auth question themselves: with JWT on, `/auth/me` returns `401`
from its own handler and `/auth/login` serves the page that says the dashboard needs the proxy.
`/metrics/prometheus` being public is deliberate for scraping and is a hazard in a multi-tenant
deployment, because nothing on it is scoped to a tenant; see [Self-hosting](/deploy/).

A second, independent exemption list is `JWT_SKIP_PATHS`, which defaults to
`/health,/metrics/prometheus,/metrics,/`. Entries match a path exactly, or as a prefix when the
entry ends in `/`. A path on that list skips authentication entirely, so widening it widens the
unauthenticated surface.

## Requests

Bodies are JSON. The broker reads the raw bytes and parses them as JSON without inspecting
`Content-Type`, so sending `application/json` is good manners rather than a requirement. A
body that does not parse returns **400** with code `bad_body` and the parser's message:

```json
{"error":"bad push body: expected value at line 1 column 1","code":"bad_body"}
```

`POST /api/v1/transaction` is the exception: a malformed bundle answers `200` with
`success:false` and `reason:"bad_request"`. See [its page](/reference/http/transaction).

Query parameters on the message-plane routes are typed, not free-form strings: `batch=abc` is a
deserialization failure for the whole query string rather than a silently-ignored parameter, and
it is rejected by the extractor before the handler runs.

The request body cap is **64 MiB** (`QUEEN_MAX_BODY_BYTES`, default `67108864`). A larger body
is rejected by the body-limit layer before any handler runs. The cap applies per request, so a
push of many small messages is bounded by the sum of their payloads plus JSON framing.

## Responses

Successful API responses carry `Content-Type: application/json`. The exception is
`/metrics/prometheus`, which returns `text/plain; version=0.0.4`.

### Every 204 has no body at all

When a pop has nothing to deliver, the broker returns `204 No Content` with **no body and no
`Content-Length`**. This is deliberate, and it is not cosmetic: announcing a content length for
a body that hyper then elides makes strict HTTP/1.1 clients (Node's `undici`/`llhttp` in
particular) treat the connection as poisoned and close it, which under empty-poll load
snowballs into `ECONNRESET` storms. The bodies that were dropped carried nothing a client read.

Consequences you have to design around:

- **Check the status code before parsing.** A 204 has no JSON to parse.
- An empty result carries no diagnostic: the client sees the same bare 204 for an idle queue,
  messages held back by a visibility delay and a long poll that timed out. A pop whose answer
  has something to say about [conflation](/reference/http/pop#conflation-in-the-response) is
  the one exception: it keeps its `200` and its body, such as
  `{"messages":[],"conflation":true, ...}` for an empty conflating pop.
- A pop that fails does not look like an empty one: it answers with an error status and a body
  (see the codes below).

### Errors

Every error body is a JSON object with an `error` string, JSON-escaped so it is always parseable.
An error raised by the state machine also carries a stable `code`, and a retry that knows the
leader names it:

```json
{"error":"not the leader; retry against 2","code":"retry","leader":"2"}
```

| Status | `code` | Meaning | `Retry-After` |
| --- | --- | --- | --- |
| `503` | `no_leader` | no leader is known (an election, or no quorum) | `1` |
| `503` | `retry` | the command was not appended; send it again (a `leader` hint may be present) | `1` |
| `503` | `timeout` | the request deadline elapsed before the answer | `1` |
| `429` | `overloaded` | the push admission budget stayed full for the whole hold | 1 to 5 s |
| `507` | `storage_full` | the data directory's filesystem is above `QUEEN_RAFT_DISK_HIGH_PCT` (default 85) used | none |
| `413` | `name_too_long` | a queue, partition or consumer group name makes a store key longer than the limit | none |
| `400` | the refusal's own code, e.g. `bad_body` | a request the client must fix, not retry | none |
| `500` | `internal` | a broker fault | none |

A `timeout` does not say whether the command committed: the write may still have applied. That is
why the retry table below matters.

Some refusals come from the route's own handler before the state machine is involved, and carry
no `code`, among them the pop refusals (`{"success":false,"error":"...","messages":[]}`).

**Push admission (`429`).** The commands that grow storage (pushes, transactions, KV puts, timer
schedules) share a byte budget, `QUEEN_RAFT_ADMIT_MAX_MB` (default 64, `0` turns it off). An HTTP
push or transaction is charged its `Content-Length` before its body is read. When the budget is
spent a new command waits for room, so a normal producer only slows down; after the hold
(`QUEEN_RAFT_ADMIT_HOLD_MS`, default 15000 ms, jittered by ±25%) it is refused with `429` and a
`Retry-After` jittered over 1 to 5 s. Acks and pops never take budget.

**Disk gate (`507`).** Writes are refused once the data directory's filesystem is
`QUEEN_RAFT_DISK_HIGH_PCT` used (default 85) and accepted again below `QUEEN_RAFT_DISK_LOW_PCT`
(default 80).

### Management routes and their shapes

Routes outside the message plane, KV, timers, the ephemeral verbs, the operational endpoints and
the Raft membership are answered by the state machine's generic adapter. It renders the response
shapes the per-route pages document, which are the shapes the 1.x broker served, so existing
clients keep working. The message-plane routes on the following pages assemble their bodies field
by field, and the fields that are *absent* are as load-bearing as the ones that are present.

### A 2xx does not mean every item succeeded

`/api/v1/push`, `/api/v1/ack` and `/api/v1/ack/batch` answer with a **top-level JSON array**,
one element per request item, in request order. The HTTP status describes the call; each
element describes one item.

| Route | Status on a processed call | Where the real outcome is |
| --- | --- | --- |
| `POST /api/v1/push` | `201` | each element's `status`: `queued`, `duplicate` or `error` |
| `POST /api/v1/ack`, `/ack/batch` | `200` | each element's `success`, `error`, `noop`, `dlq`, `leaseReleased` |
| `POST /api/v1/transaction` | `200` | top-level `success`; a rollback is `success:false` with `reason`, `error` and an empty `results` |

A client that only checks `res.ok` can lose messages: a push stores each `(queue, partition)`
group of its items as its own command, and the items of a group the state machine refused come
back as `status:"error"` inside a `201`.

### Status codes used by the message plane

| Code | When |
| --- | --- |
| `200` | a pop that delivered messages; every ack; a lease extension; a transaction that was processed, including one that rolled back |
| `201` | every push the broker processed, including one with items marked `error` |
| `204` | a pop with nothing to deliver. No body |
| `400` | unparseable body (`bad_body`), a missing required field, a discovery pop with neither `namespace` nor `task`, a conflation request the pop refuses, a planner rejection, or a malformed `x-queen-tenant` when tenancy is on |
| `401` / `403` | authentication and authorization, as above |
| `404` | a path the broker does not serve |
| `413` | the request body exceeds the cap, or a name is too long (`name_too_long`) |
| `429` | push admission (`overloaded`), on push and transaction |
| `500` | a broker fault (`internal`) |
| `503` | no leader (`no_leader`), a retry (`retry`) or an elapsed deadline (`timeout`) |
| `507` | the disk gate (`storage_full`) |

The [kv](/reference/http/kv), [timer](/reference/http/timers) and
[ephemeral](/reference/http/ephemeral) routes add limits of their own, on purpose. They are the
surfaces whose call rate is decided by an end user of the customer rather than by a volume of
messages, so a defence that lived only in a gateway would not be a defence of the product. Those
routes carry per-tenant limits in the broker itself and can answer `429` with `Retry-After`. They
also answer `403` and `503` where the message plane has no equivalent, and
[their pages](/reference/http/kv) carry the one sentence that separates the three.

## Retrying safely

| Operation | Safe to retry blindly | Why |
| --- | --- | --- |
| `POST /api/v1/push` | yes, **if** you send your own `transactionId` | deduplication is exact and window-bounded, so the retry returns `status:"duplicate"` and writes nothing |
| `GET /api/v1/pop` | no | a pop takes a lease and may deliver a different batch; retrying is a new claim, not a repeat |
| `POST /api/v1/ack` | yes | an ack that lands below the cursor is reported `noop:true` rather than double-committing |
| `POST /api/v1/lease/:leaseId/extend` | yes | renewal never shortens a lease |
| `POST /api/v1/transaction` | only with deterministic `transactionId`s | a re-sent bundle whose pushes were already stored rolls back whole on the duplicate, so nothing in it is written twice |
| `POST /api/v1/timers` | yes | schedule and reschedule are the same upsert on `(queue, timerKey)`, so a repeat replaces rather than duplicating |
| `DELETE /api/v1/timers/:queue/*timerKey` | yes | the second cancel answers `absent`, which is `ok:false` and not an error |

The KV operations differ from each other, which is the part that catches people out.

| Operation | Safe to retry blindly | Why |
| --- | --- | --- |
| `putIfAbsent` | yes | the second attempt loses the race against its own first attempt and reports `applied:false, reason:"exists"` |
| `delete` | yes | a delete that matched nothing is `applied:false`, not an error |
| `put` | only with `expect` | without a precondition it is an unconditional upsert, so a retry overwrites a value another writer put there in between |
| `get`, `getMany`, `getPrefix` | yes | reads |
| `incr` | **no** | it is the one operation with no precondition, by design, and a blind retry counts twice |

The exception under `incr` is worth stating exactly, because it is the case that makes a counter
wrong rather than an operation fail: a retry counts twice **unless the transaction that carried it
failed**. Inside `POST /api/v1/transaction` a rolled-back bundle wrote nothing, so re-sending it is
safe; standalone on `POST /api/v1/kv`, a timeout leaves you unable to tell whether the increment
committed, and there is no `expect` to make the second attempt conditional. Either put the `incr`
in a transaction whose failure you can observe, or accept that a retry may double it.

## The tenant-scoped column

The generated route table has a **Tenant-scoped** column. It records whether that handler
resolves a tenant identity for the request and hands it to the state machine, which keeps every
queue, consumer group and key under its tenant. Native tenant scoping is **off by default**; with
it off, every request resolves to a single default tenant and the column tells you which handlers
would participate if it were on. It is an operator-level feature of multi-tenant deployments, not
a client-facing one. [Self-hosting](/deploy/) and [Internals](/internals) cover it, including
which routes are *not* scoped and what that implies.

## Every route

<Render file="generated/broker-routes" />
