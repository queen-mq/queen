---
title: Overview
description: "What you build on Queen: producer and consumer, fan-out, deduplicated ingest, transactional pipelines, replay and windowed aggregation."
type: overview
tier: use
sidebar:
  order: 1
sourceOfTruth:
  - server/src/main.rs
  - server/src/handlers/data.rs
digest: >-
  The surface is push, pop and acknowledge plus one container, `POST /api/v1/transaction`, which
  commits any number of pushes and acks across any number of queues, partitions and consumer
  groups in one PostgreSQL transaction. Two optional top-level arrays ride that same commit, and
  every broker serves them: `kv`, a transactional key/value store where every
  write carries an expiry, and `timers`, a message promised now and delivered later, cancellable
  until it fires and with no recurrence. The queue and partition are created by the push that names
  them, so nothing is provisioned first. An acknowledgement commits a position, not one message,
  so acking the last message of a batch completes the batch. A consumer group holds at most one
  leased batch per partition, so parallelism comes from partitions, not from more consumers on
  one. It is all HTTP: curl is a client and the SDKs are convenience over it. Packages: JavaScript
  `npm i queen-mq`, Python `pip install queen-mq`, Go `go get
  github.com/smartpricing/queen/clients/client-go`, Rust crate `queen-mq`, PHP `composer require
  smartpricing/queen-mq`, CLI `make -C clients/client-cli build`, C++ one header
  `clients/client-cpp/queen_client.hpp`.
---

The surface you write against is small: push a message, pop it, acknowledge it. The fourth call
is not a fourth operation but a container.
[`POST /api/v1/transaction`](/reference/http/transaction/) takes an `operations` array of pushes
and acks, any number of each, spanning any number of queues, partitions and consumer groups, and
commits the whole array in one PostgreSQL transaction. Every application in this section is those
three operations and that container in a different arrangement.

The container takes two more arrays, and they are top-level fields of the request body rather than
entries in `operations`: `kv` writes [transactional state](/use/kv) and `timers` schedules or cancels
a [timer](/use/timers). Both are served by every broker, and a bundle that carries neither is byte for
byte the call it was before they existed.

[Ephemeral queues](/use/ephemeral) are the one thing here that is not built on that commit at all.
They are a second storage class, on their own routes, whose contents live in the broker's memory and
survive nothing, for the workloads that have no use for history and should not pay for it.

<Render file="snippets/js-push" />

The queue and the partition are created by the push that names them, so there is nothing to
provision first. [The model](/use/model) is one page: queue, partition, offset, consumer group,
cursor, lease, ack.

Two rules shape everything built on top. An acknowledgement is a commit of a position, not a
receipt for one message, so acking the last message of a batch completes the batch. And a
consumer group holds at most one leased batch per partition, so parallelism comes from partitions,
not from more consumers on one.

Everything is HTTP over a small JSON API, so curl is a client and the SDKs are convenience over
it. Clients hold no coordination state: a worker that restarts does not stall its peers.

What people build with it:

- **Producer and consumer.** One queue, one group, an ack when the work is done:
  [the loop](/use/examples/#producer-and-consumer).
- **Fan-out.** Two groups over one queue, each reading every message, at one copy of the data:
  [fan-out](/use/examples/#fan-out-to-two-groups).
- **Deduplicated ingest.** A deterministic `transactionId`, and the repeat refused by the broker:
  [deduplication](/use/examples/#deduplicated-ingest).
- **Transactional pipeline.** Ack the input and push the next stage in one commit:
  [the pipeline](/use/examples/#transactional-pipeline).
- **Replay.** Move a group's cursor back to a timestamp, or point a new group at history:
  [replay](/use/examples/#replay).
- **Windowed aggregation.** Tumbling, sliding, session and cron windows, with the accumulator in
  the same PostgreSQL: [streams](/use/streams).

Pick a client, write the loop, read [errors](/reference/errors/) before it meets traffic.

<CardGrid>
  <LinkCard
    href="/use/model"
    title="The model"
    description="Queue, partition, offset, consumer group, cursor, lease, ack: the whole vocabulary in one page."
  />
  <LinkCard
    href="/use/js-client"
    title="JavaScript client"
    description="Install it, connect it, push and consume: the shortest path from nothing to a running loop."
  />
  <LinkCard
    href="/use/laravel"
    title="Laravel queues"
    description="Use Queen as a Laravel queue connection, then add worker pools, a dashboard or a Horizon migration."
  />
  <LinkCard
    href="/use/examples/"
    title="Examples"
    description="Six recipes with the code that makes each one work and the rule that decides how it fails."
  />
  <LinkCard
    href="/use/http-client"
    title="HTTP client"
    description="Push, pop, acknowledge and long-poll as runnable curl, for a language with no SDK."
  />
</CardGrid>
