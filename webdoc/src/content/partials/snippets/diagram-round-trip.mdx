{/*
  The push, pop and ack round trip, shared by the hello world tutorial in
  every client. It is a partial rather than seven copies so the seven tracks
  cannot drift apart, and it names no language for the same reason.

  Its one job is the thing the prose has to say twice otherwise: a pop does
  not remove and an ack does not delete. Both are moves on a cursor.
*/}

<pre class="mermaid">{`
sequenceDiagram
  participant C as client
  participant B as broker
  participant L as replicated log
  C->>B: push, naming a queue and a partition
  B->>L: one entry: create both if absent, append the message at the next offset
  L-->>B: committed
  B-->>C: 201, per-item status queued
  C->>B: pop, with a batch size and a wait
  B->>L: one entry: claim the partition under a lease
  L-->>B: committed
  B-->>C: 200, messages from committed + 1 and a leaseId
  C->>B: ack, by transactionId and partitionId
  B->>L: one entry: move committed past it, release the lease
  L-->>B: committed
  B-->>C: 200, success true on the item
  Note over L: nothing was deleted.<br/>The cursor moved.
`}</pre>
